rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TEMPORARY: Allow all authenticated users to read and write all data.
      // This is for debugging the client-side permissions error and is NOT secure.
      allow read, write: if request.auth != null;
    }
  }
}